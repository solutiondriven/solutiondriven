import { projectId } from '../../../utils/supabase/info';

export const supabaseUrl = `https://${projectId}.supabase.co`;
export const serverFunctionBaseUrl = `${supabaseUrl}/functions/v1/server/make-server-3e6a90b9`;
